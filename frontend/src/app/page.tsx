// Intentionally empty — the actual chat UI is rendered by AppShell
// (layout.tsx) so it stays mounted across route changes. See
// components/app-shell.tsx for why.
export default function Home() {
  return null;
}
